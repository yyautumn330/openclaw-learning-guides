// @keepTs
// @ts-nocheck
/**
 * 坡度消耗权值
 *
 * @exclude javadoc中不包含
 */
export declare class y71 {
    /**
     * 坡度上升的消耗系数，坡度上升导致的势能提升与车辆耗电量间的比例关系，势能增量/SLOPE_UP=耗电量，单位J/KWH，取值范围[0-3600000]
     */
    private up;
    /**
     * 坡度下降的回收系数，坡度下降导致的势能减少与车辆回收电量间的比例关系，势能减量/SLOPE_DOWN=回收电量，单位J/KWH，取值范围暂无限制
     */
    private down;
    /**
     * 获取坡度上升的消耗系数，坡度上升导致的势能提升与车辆耗电量间的比例关系，势能增量/SLOPE_UP=耗电量，单位J/KWH，取值范围[0-3600000]
     *
     * @return 坡度上升的消耗系数，坡度上升导致的势能提升与车辆耗电量间的比例关系，势能增量/SLOPE_UP=耗电量，单位J/KWH，取值范围[0-3600000]
     * @exclude javadoc中不包含
     */
    o73(): number;
    /**
     * 设置坡度上升的消耗系数，坡度上升导致的势能提升与车辆耗电量间的比例关系，势能增量/SLOPE_UP=耗电量，单位J/KWH，取值范围[0-3600000]
     *
     * @param up 坡度上升的消耗系数，坡度上升导致的势能提升与车辆耗电量间的比例关系，势能增量/SLOPE_UP=耗电量，单位J/KWH，取值范围[0-3600000]
     * @exclude javadoc中不包含
     */
    t85(up: number): void;
    /**
     * 获取坡度下降的回收系数，坡度下降导致的势能减少与车辆回收电量间的比例关系，势能减量/SLOPE_DOWN=回收电量，单位J/KWH，取值范围暂无限制
     *
     * @return 坡度下降的回收系数，坡度下降导致的势能减少与车辆回收电量间的比例关系，势能减量/SLOPE_DOWN=回收电量，单位J/KWH，取值范围暂无限制
     * @exclude javadoc中不包含
     */
    p73(): number;
    /**
     * 设置坡度下降的回收系数，坡度下降导致的势能减少与车辆回收电量间的比例关系，势能减量/SLOPE_DOWN=回收电量，单位J/KWH，取值范围暂无限制
     *
     * @param down 坡度下降的回收系数，坡度下降导致的势能减少与车辆回收电量间的比例关系，势能减量/SLOPE_DOWN=回收电量，单位J/KWH，取值范围暂无限制
     * @exclude javadoc中不包含
     */
    u85(down: number): void;
}
